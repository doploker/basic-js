# Базовый JavaScript

https://learn.javascript.ru/?ysclid=mhonoj1ij1931109318

## Основы програмирования
### Типы данных:
В JavaScript есть 8 основных типов данных и практических других ЯП.

Семь из них называют «примитивными» типами данных:
- number для любых чисел: целочисленных или чисел с плавающей точкой; целочисленные значения ограничены диапазоном ±(253-1).
- bigint для целых чисел произвольной длины.
- string для строк. Строка может содержать ноль или больше символов, нет отдельного символьного типа.
- boolean для true/false.
null для неизвестных значений – отдельный тип, имеющий одно значение null.
- undefined для неприсвоенных значений – отдельный тип, имеющий одно значение undefined.
- symbol для уникальных идентификаторов.
И один не является «примитивным» и стоит особняком:
- object для более сложных структур данных.
Оператор typeof позволяет нам увидеть, какой тип данных сохранён в переменной.

Имеет две формы: typeof x или typeof(x).
Возвращает строку с именем типа. Например, "string".
Для null возвращается "object" – это ошибка в языке, на самом деле это не объект.

## Основы JavaScript

### Вывод данных
```js
//вывод данных при помощи диалогового окна
alert('fljoe')
//вывод в консоль
console.log('')
```


### Условное вветвление
```js
if(true){
    операции
} else if(false){
    доп.проверка
} else{
    если не подходит не одно условие
}
``` 

### Циклы

while
```js
let i = 0;
while (i < 3) { // выводит 0, затем 1, затем 2
  alert( i );
  i++;
}
```

for
```js
for (начало; условие; шаг) {
  // ... тело цикла ...
}

for (let i = 0; i < 3; i++) { // выведет 0, затем 1, затем 2
  alert(i);
}
```

Прерывание цикла: «break»
```js
let sum = 0;

while (true) {

  let value = +prompt("Введите число", '');

  if (!value) break; // (*)

  sum += value;

}
alert( 'Сумма: ' + sum );
```

Переход к следующей итерации: continue
```js
for (let i = 0; i < 10; i++) {

  // если true, пропустить оставшуюся часть тела цикла
  if (i % 2 == 0) continue;

  alert(i); // 1, затем 3, 5, 7, 9
}
```

### Функции
Объявление функции
```js
function showMessage() {
  alert( 'Всем привет!' );
}

function имя(параметры) {
  ...тело...
}

//Наша новая функция может быть вызвана по своему имени: showMessage().

showMessage();
showMessage();
```

Локальные переменные
```js
function showMessage() {
  let message = "Привет, я JavaScript!"; // локальная переменная

  alert( message );
}

showMessage(); // Привет, я JavaScript!

alert( message ); // <-- будет ошибка, т.к. переменная видна только внутри функции
```

Внешние переменные
```js
let userName = 'Вася';

function showMessage() {
  let message = 'Привет, ' + userName;
  alert(message);
}

showMessage(); // Привет, Вася
```

### Function Expression
```js
//первый способ записи функции
function sayHi() {
  alert( "Привет" );
}

// второй способ Function Expression (Функциональное Выражение).
let sayHi = function() {
  alert( "Привет" );
};

//третий способ анонимная функция
let f = () => {
    alert( "Привет" );
}
```

### constructor switch
```js
let age = prompt('Сколько вам лет?', 18);

switch (age) {
  case 18:
    alert("Так не сработает"); // результатом prompt является строка, а не число

  case "18":
    alert("А так сработает!");
    break;

  default:
    alert("Любое значение, неравное значению выше");
}
```

### Обьекты
```js
let user = {
  name: "John",
  age: 30
};
//получить массив ключей
Object.keys(user) = ["name", "age"]

//получить массив значений
Object.values(user) = ["John", 30]

//получить массив пар
Object.entries(user) = [ ["name","John"], ["age",30] ]

```